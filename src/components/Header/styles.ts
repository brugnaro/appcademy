import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: '#215897',
    borderBottomColor: '#F8D117',
    borderBottomWidth: 4,
    flexDirection: 'row',
    justifyContent: 'space-between',
    paddingHorizontal: 20,
    paddingVertical: 20,
  },
  text: {
    color: '#FFF',
    fontSize: 18,
    paddingRight: 20,
  },
});

export default styles;

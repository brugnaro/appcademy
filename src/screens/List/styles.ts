import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {flex: 1, paddingBottom: 90},
  text: {color: '#a2a2a2', fontSize: 14, paddingLeft: 20, paddingVertical: 10},
});

export default styles;
